import React, { ReactNode } from "react";
import "./Contact.scss";

class Contact extends React.Component {
  render(): ReactNode {
    return (
      <section id="contact">
        <h1>Contact</h1>
      </section>
    );
  }
}

export default Contact;
