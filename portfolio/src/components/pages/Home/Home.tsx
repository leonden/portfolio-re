import React, { ReactNode } from "react";
import "./Home.scss";

class Home extends React.Component {
  render(): ReactNode {
    return (
      <section id="home">
        <h1>home</h1>
      </section>
    );
  }
}

export default Home;
