import React, { ReactNode } from "react";
import "./Work.scss";

class Work extends React.Component {
  render(): ReactNode {
    return (
      <section id="work">
        <h1>Work</h1>
      </section>
    );
  }
}

export default Work;
